baseURL = "https://pgloader.io/"
languageCode = "en-us"
title = "pgloader"

publishDir = "docs"

# Vendored 1:1 from ~/dev/web/oss.taop.xyz/themes/oss — same product-page
# theme oss.theartofpostgresql.com's own /pgloader/ project page already
# uses (hero, value-props, pricing table), applied here on pgloader.io
# itself instead of only existing as a page on a sibling site.
theme = "oss"

[params]
  description = "pgloader v4 — a single Java JAR that migrates MySQL, MS SQL Server, SQLite, and DBF to PostgreSQL in one command."
  github = "https://github.com/dimitri/pgloader"
  email = "dim@tapoueh.org"
  custom_css = ["css/custom.css"]

[markup.goldmark.renderer]
  unsafe = true
